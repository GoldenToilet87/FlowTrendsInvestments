/* General body styling */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #000000;
    color: white;
}

/* Hidden element */
.hidden {
    display: none;
}

/* Admin-only hidden links */
.admin-only {
    display: none;
}

/* Navbar styling */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
    background-color: #000000;
    position: relative;
}

/* Left section with logo and profile button */
.left-section {
    display: flex;
    align-items: center;
}

/* Dropdown Logo Styling with hover spin effect */
.logo-dropdown img {
    width: 50px;
    height: 50px;
    cursor: pointer;
    transition: transform 0.5s ease;
}

/* Spaced out "FlowTrend Investments" name next to the logo */
.logo-text {
    font-size: 1.2em;
    font-weight: bold;
    color: white;
    margin-left: 20px;
    letter-spacing: 0.1em;
    cursor: pointer;
    transition: color 0.3s ease;
}

/* Cyan selection effect */
::selection {
    background-color: #00ffff;
    color: #ff0080;
}

/* Spin effect on hover for logo */
.logo-dropdown img:hover {
    transform: rotate(360deg);
}

/* Hover effect for FlowTrend Investments name */
.logo-text:hover {
    color: #00ffff;
}

/* Dropdown Menu Styling */
.dropdown-menu {
    display: none;
    position: absolute;
    top: 100%;
    left: 0;
    background-color: #000000;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    z-index: 100;
    width: 180px;
}

.dropdown-menu a {
    display: block;
    padding: 10px 20px;
    color: white;
    text-decoration: none;
}

.dropdown-menu a:hover {
    background-color: #00ffff;
    color: #ff0080;
}

.dropdown-menu.active {
    display: block;
}

/* Centered Menu Styling */
.centered-menu {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 20px;
}

.centered-menu a {
    text-decoration: none;
    color: #ffffff;
    font-size: 1em;
    transition: color 0.3s ease;
}

.centered-menu a:hover {
    color: #ff0080;
    background-color: #00ffff;
    padding: 5px;
    border-radius: 5px;
}

/* Hero section styling */
.hero {
    background-color: #000000;
    text-align: center;
    padding: 100px 20px;
}

.cta-button {
    padding: 15px 30px;
    background-color: #ff0080;
    color: white;
    border: none;
    font-size: 1.2em;
    cursor: pointer;
    margin-top: 20px;
    border-radius: 8px;
}

.cta-button:hover {
    background-color: #ff0050;
}

/* Content section styling */
.content-section {
    padding: 80px 20px;
    background-color: #000000;
    text-align: center;
}

.content-section h2,
.content-section p {
    color: white;
}

/* Social media icons styling */
.social-icons {
    display: flex;
    gap: 15px;
    margin-left: auto;
}

.social-icons a {
    color: white;
    font-size: 24px;
    padding: 15px;
    background-color: transparent;
    border-radius: 50%;
    transition: transform 0.3s ease, background-color 0.3s ease, box-shadow 0.3s ease;
    text-decoration: none;
    box-shadow: none;
}

/* Hover effect for social media icons */
.social-icons a:hover {
    transform: scale(1.1);
    background-color: #ff0080;
    color: #00ffff;
    box-shadow: 0 0 8px rgba(0, 255, 255, 0.5);
}

/* Spin effect on hover for social media icons */
.social-icons a:hover i,
.social-icons a:hover img {
    transform: rotate(360deg);
    transition: transform 0.5s ease;
}

/* Membership Section Styling */
.membership-plans {
    display: flex;
    justify-content: space-around;
    margin-top: 40px;
}

.plan {
    background-color: #1a1a1a;
    padding: 20px;
    border-radius: 10px;
    text-align: center;
    width: 20%;
}

.plan h3 {
    color: #00ffff;
}

.plan p {
    color: #ffffff;
    margin: 10px 0;
}

.subscribe-btn {
    background-color: #ff0080;
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 5px;
    cursor: pointer;
    margin-top: 10px;
}

.subscribe-btn:hover {
    background-color: #ff0050;
}

/* Payment Options Styling */
.payment-options {
    margin-top: 40px;
    text-align: center;
}

.payment-options h3 {
    color: #00ffff;
}

.payment-methods {
    display: flex;
    justify-content: space-around;
    margin-top: 20px;
    font-size: 1.5em;
    color: white;
}

.payment-methods i {
    margin-right: 10px;
}

/* Admin Login Section */
#admin-login-section {
    background-color: #1a1a1a;
    padding: 40px;
    width: 300px;
    margin: 50px auto;
    border-radius: 10px;
    text-align: center;
}

#admin-login-section h2 {
    color: #00ffff;
}

#admin-login-section input {
    padding: 10px;
    margin: 10px 0;
    width: 100%;
    border-radius: 5px;
    border: none;
    background-color: #333;
    color: #fff;
}

#admin-login-section button {
    padding: 10px 20px;
    background-color: #ff0080;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    margin-top: 10px;
}

#admin-login-section button:hover {
    background-color: #ff0050;
}

/* Token Swap Section */
#token-swap {
    margin-top: 50px;
}

.swap-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    background-color: #1a1a1a;
    padding: 30px;
    border-radius: 10px;
    width: 300px;
    margin: 0 auto;
}

.swap-container select, 
.swap-container input {
    padding: 10px;
    width: 100%;
    border-radius: 5px;
    border: none;
    background-color: #333;
    color: #fff;
}

#transaction-status {
    text-align: center;
    color: #00ff00;
    margin-top: 20px;
}
